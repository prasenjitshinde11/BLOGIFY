services:
  - type: web
    name: blogify-jitt
    runtime: python
    buildCommand: pip install -r requirements.txt && flask db upgrade
    startCommand: gunicorn fast:app
    envVars:
      - key: FLASK_APP
        value: fast.py
      - key: PYTHON_VERSION
        value: "3.10.12"
